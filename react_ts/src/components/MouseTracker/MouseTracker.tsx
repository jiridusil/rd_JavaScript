import { ReactNode } from "react";

export const MouseTracker = (
    { render }: { render: (input: { x: number, y: number }) => ReactNode; }) => {
    return (
        <div>hello world </div>
    )
}
